/*
使用方法的时候，注意事项：

1.方法应该定义在类当中，但是不能在方法中再定义方法，方法不能嵌套。
2.方法定义的前后顺序无所谓。
3.方法定义之后不会执行，如果希望执行，一定要调用。
4.如果方法有返回值，那么必须写上return+返回值，不能没有。
5.return 后的返回值类型必须和方法定义时的方法类型保持一致。
6.对于void类型的方法，方法中可胜率return 。
7.当一个方法中有多个return语句时，但必须同时保证只有一个会被执行到。
*/
综上所述，java中的方法和c语言中的函数差不多。
